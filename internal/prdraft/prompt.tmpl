You are drafting a pull request for issue #{{.IssueNumber}} in {{.Repo}}. Examine the differences from the main branch.

Steps:
1. Run git status to understand the current branch
2. Run git log --oneline main..HEAD to see the commits
3. Run git diff --stat main..HEAD for a file-level summary
4. Read any files you need to understand the changes (use git diff main..HEAD or read source files)
5. Search for a PR template — check .github/pull_request_template.md, .github/PULL_REQUEST_TEMPLATE.md, docs/pull_request_template.md, pull_request_template.md, and .github/PULL_REQUEST_TEMPLATE/ directory. Use Glob to find them.

If a PR template exists, follow its structure. Otherwise use this format:

## Why
[Problem or motivation — 1-3 sentences]

## What
[Approach taken — brief, not a code walkthrough]

## Notes for reviewers
[Non-obvious decisions, areas of uncertainty, or things worth calling out]

Title rules:
- Conventional commit format (e.g. "feat(scope): description")
- Concise — under 72 characters before the emoji
- End with a mood emoji that fits the vibe of the changes

Do NOT include "Closes #{{.IssueNumber}}" in the body — the system adds that automatically.

When you are done, provide your PR title and body using the structured output tool. Do not just write them in your response text.
